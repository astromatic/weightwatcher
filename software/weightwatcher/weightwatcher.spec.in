%define name @PACKAGE_NAME@
%define version @PACKAGE_VERSION@
%define release 1

Summary: combine weight-maps, flag-maps and polygon data
Name: %{name}
Version: %{version}
Release: %{release}
Source0: ftp://ftp.iap.fr/pub/from_users/bertin/%{name}/%{name}-%{version}.tar.gz
URL: http://terapix.iap.fr/soft/%{name}/
License: LGPL
Group: Sciences/Astronomy
BuildRoot: %{_tmppath}/%{name}-buildroot
Prefix: %{_prefix}

%description
WeightWatcher is a program that combines weight-maps, flag-maps and
polygon data in order to produce control maps which can directly be
used in astronomical image-processing packages like Swarp or SExtractor.

%prep
%setup -q

%build
if test "$TPXFLAGS"; then
./configure --enable-static --prefix=$RPM_BUILD_ROOT/usr/local/ --mandir=$RPM_BUILD_ROOT/usr/local/man/ --with-xsl_url=file:///usr/local/share/@PACKAGE_NAME@/ww.xsl 
make CC="$TPXCC" CFLAGS="$TPXFLAGS"
else
./configure --prefix=$RPM_BUILD_ROOT/usr/local/ --mandir=$RPM_BUILD_ROOT/usr/local/man/ --with-xsl_url=file:///usr/local/share/@PACKAGE_NAME@/ww.xsl
make
fi

%install
make install

%clean
rm -rf $RPM_BUILD_ROOT

%files
%defattr(-,root,root)
/usr/local/bin/ww
/usr/local/man/man1/ww.1
/usr/local/man/manx/ww.x
/usr/local/share/@PACKAGE_NAME@
%doc AUTHORS BUGS ChangeLog COPYING HISTORY INSTALL README THANKS

%changelog
* @DATE2@ @PACKAGER@ <@PACKAGE_BUGREPORT@>
- Automatic RPM rebuild
* Tue Apr 13 2004 Emmanuel Bertin <bertin@iap.fr>
- Second RPM build
* Sun Jan 12 2003 Emmanuel Bertin <bertin@iap.fr>
- First RPM build

# end of file
